# === Dropbox (shared data sync) ===
DROPBOX_APP_KEY=
DROPBOX_APP_SECRET=
DROPBOX_REFRESH_TOKEN=

# === Volcengine 火山引擎 (Chat LLM) ===
# LiteLLM reads VOLCENGINE_API_KEY automatically for volcengine/ models.
VOLCENGINE_API_KEY=
VOLCENGINE_API_BASE=https://ark.cn-beijing.volces.com/api/v3

# === AIHUBMIX (Embedding via litellm_proxy) ===
# RD-Agent requires litellm_proxy/ prefix when embedding uses a different
# provider from chat.  The actual AIHUBMIX routing is done via these vars.
LITELLM_PROXY_API_KEY=
LITELLM_PROXY_API_BASE=https://aihubmix.com/v1

# === RD-Agent LLM Backend ===
BACKEND=rdagent.oai.backend.LiteLLMAPIBackend
CHAT_MODEL=volcengine/glm-4.7
EMBEDDING_MODEL=litellm_proxy/text-embedding-3-small

# Fallback: some RD-Agent code paths still read OPENAI_API_KEY.
# Set this to your volcengine key as a safe fallback.
OPENAI_API_KEY=

# === Paths (WSL2 native paths for production) ===
QLIB_DATA_PATH=~/qlib_rd_agent/data/qlib
RDAGENT_WORKSPACE=~/qlib_rd_agent/workspace

# === Conda (RD-Agent runs in a separate conda env) ===
CONDA_ENV_NAME=rdagent4qlib
